/*
Before you can debug:

Ensure pgAdmin is connected to a PostgreSQL server that has the plpgsql language installed.

The pldbgapi extension must be enabled on the database:

*/
CREATE EXTENSION IF NOT EXISTS pldbgapi;
